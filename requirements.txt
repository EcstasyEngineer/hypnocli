# Core dependencies
openai>=1.0.0
boto3>=1.26.0
python-dotenv>=1.0.0

# TTS rendering
pydub>=0.25.0
soundfile>=0.12.0

# TTS rendering — Windows only (render_winrt.py)
# winrt-runtime>=3.0.0
# winrt-Windows.Media.SpeechSynthesis>=3.0.0
# winrt-Windows.Storage.Streams>=3.0.0

# Audio processing (requires system sox: sudo apt-get install sox libsox-fmt-all)
sox>=1.5.0

# Hypernym decontamination (quality gates)
nltk>=3.8.0

# Analysis (optional)
numpy>=1.24.0

# GLSL adversarial pipeline (glsl/)
anthropic>=0.40.0
playwright>=1.40.0
# After installing playwright: python3 -m playwright install chromium
